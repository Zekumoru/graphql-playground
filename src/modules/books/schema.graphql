type Query {
  books(minPages: Int): [Book!]!
}

type Book {
  title: String!
  pages: Int!
  author: Author!
}

type Mutation {
  addBook(input: AddBookInput!): Book!
}

type Subscription {
  bookAdded: Book!
}

input AddBookInput {
  title: String!
  pages: Int!
  authorId: String!
}
